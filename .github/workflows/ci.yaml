name: ci 

on: [push]

jobs:
    build:
     runs-on: ubuntu-latest
     steps:
    - name: checkout code
      uses: actions/checkout@v4
    - name: we are testing thisworkflow
      run: echo hello world